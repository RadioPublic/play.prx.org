import { Component } from '@angular/core';

@Component({
  selector: 'play-builder',
  styleUrls: ['builder.component.css'],
  template: `
    this is the builder template
  `
})

export class BuilderComponent {
}
